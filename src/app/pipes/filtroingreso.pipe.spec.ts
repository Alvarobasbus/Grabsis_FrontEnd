import { FiltroingresoPipe } from './filtroingreso.pipe';

describe('FiltroingresoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroingresoPipe();
    expect(pipe).toBeTruthy();
  });
});
