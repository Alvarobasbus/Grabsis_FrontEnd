import { FiltroParaOrdenarPipe } from './filtro-para-ordenar.pipe';

describe('FiltroParaOrdenarPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroParaOrdenarPipe();
    expect(pipe).toBeTruthy();
  });
});
