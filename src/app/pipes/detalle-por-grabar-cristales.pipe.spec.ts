import { DetallePorGrabarCristalesPipe } from './detalle-por-grabar-cristales.pipe';

describe('DetallePorGrabarCristalesPipe', () => {
  it('create an instance', () => {
    const pipe = new DetallePorGrabarCristalesPipe();
    expect(pipe).toBeTruthy();
  });
});
