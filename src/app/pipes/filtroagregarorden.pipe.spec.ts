import { FiltroagregarordenPipe } from './filtroagregarorden.pipe';

describe('FiltroagregarordenPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroagregarordenPipe();
    expect(pipe).toBeTruthy();
  });
});
