import { FiltroListadoFormulariosPipe } from './filtro-listado-formularios.pipe';

describe('FiltroListadoFormulariosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroListadoFormulariosPipe();
    expect(pipe).toBeTruthy();
  });
});
