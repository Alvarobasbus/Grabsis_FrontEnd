import { IngresoconfirmadoPipe } from './ingresoconfirmado.pipe';

describe('IngresoconfirmadoPipe', () => {
  it('create an instance', () => {
    const pipe = new IngresoconfirmadoPipe();
    expect(pipe).toBeTruthy();
  });
});
