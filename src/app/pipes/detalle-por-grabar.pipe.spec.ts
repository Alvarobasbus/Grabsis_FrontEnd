import { DetallePorGrabarPipe } from './detalle-por-grabar.pipe';

describe('DetallePorGrabarPipe', () => {
  it('create an instance', () => {
    const pipe = new DetallePorGrabarPipe();
    expect(pipe).toBeTruthy();
  });
});
