import { IngresopagadoPipe } from './ingresopagado.pipe';

describe('IngresopagadoPipe', () => {
  it('create an instance', () => {
    const pipe = new IngresopagadoPipe();
    expect(pipe).toBeTruthy();
  });
});
