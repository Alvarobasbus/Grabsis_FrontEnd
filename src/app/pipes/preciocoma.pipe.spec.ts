import { PreciocomaPipe } from './preciocoma.pipe';

describe('PreciocomaPipe', () => {
  it('create an instance', () => {
    const pipe = new PreciocomaPipe();
    expect(pipe).toBeTruthy();
  });
});
