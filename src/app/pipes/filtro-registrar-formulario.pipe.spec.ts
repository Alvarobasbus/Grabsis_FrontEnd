import { FiltroRegistrarFormularioPipe } from './filtro-registrar-formulario.pipe';

describe('FiltroRegistrarFormularioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroRegistrarFormularioPipe();
    expect(pipe).toBeTruthy();
  });
});
