import { FiltroOrdenesPipe } from './filtro-ordenes.pipe';

describe('FiltroOrdenesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroOrdenesPipe();
    expect(pipe).toBeTruthy();
  });
});
