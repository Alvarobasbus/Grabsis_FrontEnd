import { VehiculograbadoPipe } from './vehiculograbado.pipe';

describe('VehiculograbadoPipe', () => {
  it('create an instance', () => {
    const pipe = new VehiculograbadoPipe();
    expect(pipe).toBeTruthy();
  });
});
