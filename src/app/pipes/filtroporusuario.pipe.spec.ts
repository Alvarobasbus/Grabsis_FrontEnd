import { FiltroporusuarioPipe } from './filtroporusuario.pipe';

describe('FiltroporusuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroporusuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
